public class FishFactory 
{
	private static int id = 1;
	
	public static Fish generateFish(FishType type)
	{
		switch (type) 
		{
			case SHARK: return new Shark(id++);
			case GOLDENFISH: 
			default: return new GoldenFish(id++);
		}
	}
}


public class CasherFactory {
	private final double discount = 0.8;
	
	public double generateCasher(CasherType type, CustomerOrder order)
	{
		switch (type)
		{
			case Discount: return order.getTotalPrice() * discount;
			case Normal: 
			default: return order.getTotalPrice();
		}
	}
}

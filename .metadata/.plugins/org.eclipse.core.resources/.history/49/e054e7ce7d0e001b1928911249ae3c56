import java.util.UUID;

public class FishFactory 
{
	public static Fish generateFish(FishType type)
	{
		String fishId = UUID.randomUUID().toString();
		
		switch (type) 
		{
			case SHARK: return new Shark(fishId);
			case GOLDFISH: 
			default: return new GoldFish(fishId);
		}
	}
}
